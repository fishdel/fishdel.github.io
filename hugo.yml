baseURL: "https://fishdel.github.io/" # 主站的 URL
title: Yuuuuu's Blog # 站点标题
copyright: "[©2024 Yuuuu's Blog](https://fishdel.github.io/)" # 网站的版权声明，通常显示在页脚
theme: PaperMod # 主题
languageCode: zh-cn # 语言

enableInlineShortcodes: true # shortcode，类似于模板变量，可以在写 markdown 的时候便捷地插入，官方文档中有一个视频讲的很通俗
hasCJKLanguage: true # 是否有 CJK 的字符
enableRobotsTXT: true # 允许生成 robots.txt
buildDrafts: false # 构建时是否包括草稿
buildFuture: false # 构建未来发布的内容
buildExpired: false # 构建过期的内容
enableEmoji: true # 允许 emoji
pygmentsUseClasses: true
defaultContentLanguage: zh # 顶部首先展示的语言界面
defaultContentLanguageInSubdir: false # 是否要在地址栏加上默认的语言代码

minify:
    disableXML: true
    # minifyOutput: true

permalinks:
  post: "/:title/"
  # post: "/:year/:month/:day/:title/"

params:
  homeInfoParams:
    Title: '一个学习、思考和吐槽的地方' # 主题
    Content: '写一些乱七八糟的东西，然后觉得做一个网站比较有趣
                    ' # 描述

    
   


  # 搜索
  fuseOpts:
      isCaseSensitive: false # 是否大小写敏感
      shouldSort: true # 是否排序
      location: 0
      distance: 1000
      threshold: 0.4
      minMatchCharLength: 0
      # limit: 10 # refer: https://www.fusejs.io/api/methods.html#search
      keys: ["title", "permalink", "summary", "content"]
      includeMatches: true
  assets:
      favicon: "favicon.png"
      favicon16x16: "favicon.png"
      favicon32x32: "favicon.png"
      apple_touch_icon: "favicon.png"
      safari_pinned_tab: "favicon.png"
  socialIcons:
        - name: github
          url: 'https://github.com/yunyit'
        - name: bilibili
          url: "https://space.bilibili.com/10831667"
        - name: email
          url: 'mailto:1029948143@qq.com'

  
      
languages:
  zh:
    languageName: "Chinese" # 展示的语言名
    weight: 1 # 权重
    profileMode:
    menus:
      main:
        - name: 🏠主页
          pageRef: /
          weight: 2 # 控制在页面上展示的前后顺序
        - name: 📚文章
          pageRef: posts/
          weight: 4 # 控制在页面上展示的前后顺序
        - name: ⏱时间轴
          pageRef: archives/
          weight: 5
        - name: 🧩分类
          pageRef: categories/
          weight: 10
        - name: 🔖标签
          pageRef: tags/
          weight: 10
        - name: 🔍搜索
          pageRef: search/
          weight: 20
        - name: 🙋🏻‍♂️关于
          pageRef: about/
          weight: 21
 
# https://github.com/adityatelange/hugo-PaperMod/wiki/Features#search-page
outputs:
  home:
    - HTML # 生成的静态页面
    - RSS # 
    - JSON # necessary for search, 这里的配置修改好之后，一定要重新生成一下
    



